# frozen_string_literal: true

source 'https://rubygems.org'

ruby '3.1.2'

# code styling
gem 'rubocop', require: false
# validation for plain ruby object
gem 'validy', require: true
# singleton implementation
gem 'singleton', require: true
# debugging
gem 'pry'
